# What is the difference between `print()` and `return`?
"""
| Feature     | `print()`                         | `return`                               |
| ----------- | --------------------------------- | -------------------------------------- |
| Purpose     | Displays output on the screen 🖨️ | Sends result **back to the caller** 🔁 |
| Used for    | Debugging, showing output         | Passing values between functions       |
| Output type | Nothing (`None`)                  | Actual value (can be stored or reused) |
| Scope       | Ends at the screen                | Continues in code logic                |
"""